export type Tariff = {
  id: number;
  name: string;
  maxGroups: number;
  maxKeywords: number;
  maxAccounts: number;
  cost: number;
  description: string;
  benefits: string;
  duration: number;
};
